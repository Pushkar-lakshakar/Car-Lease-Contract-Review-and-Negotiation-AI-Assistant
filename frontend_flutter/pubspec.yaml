name: car_lease_analyzer
description: Car Lease Contract Analyzer
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.1.0
  file_picker: ^6.1.2
  percent_indicator: ^4.2.3
  flutter_dotenv: ^5.1.0
  provider: ^6.1.1
  flutter_svg: ^2.0.10
  intl: ^0.19.0
  flutter_markdown: ^0.7.3
  web_socket_channel: ^3.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3

flutter:
  uses-material-design: true
